---
type: HLR
tags:
  - req/hlr
  - tui
  - domain/workspace
---
A single-row top bar at the top of the TUI carries: a WS-status dot (green/amber/red), a textual status label, and a compact hint string for the supported global keys (`a` add column, `x` remove focused column, `Tab` cycle focus, `M` toggle mouse, `?` toggle log overlay, `q` quit).

## Derived LLRs
- [[TUI top bar exposes WS status dot]]
- [[TUI top bar appends column via 'a' keystroke]]
- [[TUI top bar shows hint string for global keys]]
