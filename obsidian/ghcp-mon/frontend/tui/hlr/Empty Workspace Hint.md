---
type: HLR
tags:
  - req/hlr
  - tui
  - domain/workspace
---
When the workspace has zero columns, the TUI renders the literal text `"no columns. add one from the top bar."` in the workspace area instead of the column layout. Identical copy to the webui per `Empty workspace shows empty state`.

## Derived LLRs
- [[TUI empty workspace renders hint text]]
